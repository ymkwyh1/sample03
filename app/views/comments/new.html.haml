.container
  = form_with(model: @comment, url: post_comments_path, method: 'post',  local: true) do |f|
    .post_header
      = link_to post_comments_path(post_id: params[:post_id]) do
        %p.back ←
      = f.submit 'Post', class: 'post_submit'
    = render 'commons/card', user: current_user
    = f.text_field :content, placeholder: "What's on our mind?", class: "post_input"
