.container
  .profile_header
    = link_to account_path(id: @user.id) do
      %p.profile_back ←
    %p.profile_username Followers
  - @followers.each do |follower|
    = render 'commons/card', user: follower